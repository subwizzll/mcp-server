# ─── Open Brain MCP Server ──────────────────────────────────────────────────

# ─── DB Backend ──────────────────────────────────────────────
# "supabase" (default) or "postgres"
# DB_BACKEND=supabase

# ─── Supabase (used when DB_BACKEND=supabase, the default) ───
SUPABASE_URL="https://your-project.supabase.co"
# Use the service role key so the MCP server can read and write brain_memories
SUPABASE_SERVICE_ROLE_KEY="your_supabase_service_role_key"
SUPABASE_ANON_KEY="your_supabase_anon_key"

# ─── Raw Postgres (used when DB_BACKEND=postgres) ────────────
# DATABASE_URL="postgresql://user:password@host:5432/dbname"

# ─── OpenRouter (Embeddings) ──────────────────────────────────
OPENROUTER_API_KEY="your_openrouter_api_key"

# ─── Embedding Config ─────────────────────────────────────────
EMBEDDING_MODEL="openai/text-embedding-3-small"
EMBEDDING_DIMENSIONS=1536

# ─── SSE/HTTP Transport ───────────────────────────────────────
# Port for the HTTP/SSE server (dev:http / start:http)
MCP_HTTP_PORT=3100
